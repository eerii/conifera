{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Phonebook Backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildFrontend": "rm -rf build && cd phonebook-frontend && npm run build --prod && mv -f build ../ && cd .."
  },
  "author": "José Pazos Pérez",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.16",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
